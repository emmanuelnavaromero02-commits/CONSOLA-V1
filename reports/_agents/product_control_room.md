# Auditoría Product Functionality — Control Room / ΩMEGA Decision Cycle

Repo: `/home/user/CONSOLA-BETA` @ main `bab2a4f` (VERSION 1.45.68-beta). Pregunta central: **si el usuario hace clic, ¿pasa algo REAL?**
Método: lectura de código + SQL, sin servidores. Evidencia = `file:line`.

**Veredicto global: el ciclo ΩMEGA es mayoritariamente REAL como máquina de estados auditada sobre Postgres, con dos núcleos parciales/cosméticos: (a) el "dry-run" no valida nada (siempre `validated: true`), (b) las opciones de items no-intelligence tienen scores/riesgos hardcodeados (92/68/45). El write-back externo está bloqueado por flag, registrado como `blocked` y NUNCA presentado como ejecutado — el diseño del finding P1-CR-001 se cumple.**

---

## 1. Tabla del ciclo (etapa → veredicto)

| Etapa | Veredicto | Evidencia | Qué falta |
|---|---|---|---|
| **Señales** | **REAL** | Dos rutas, ambas data-driven: (1) anomalías por dataset Gold con RLS: `intelligence/gold_fetcher.py:79-130` (query directa `gold_*` con `set_config app.tenant_id/workspace_id`), fallback Refinement `/mcp/invoke query_dataset` `control_room/api.py:83-106`; fuentes declaradas en `control_room/core.py:371-918` (29+ datasets: `employees_anomalies`, `pnl_mensual`, `revenue_by_customer`…). (2) Intelligence Engine: `intelligence/engine.py:39-131` lee contratos YAML (`intelligence/contracts.py:49-63`; `console/app/config/intelligence_contracts/*.yaml` + `cartridges/*/app/config/intelligence.yaml`), baseline media móvil + desviación real `intelligence/baseline.py:70-117`, severidad por regla `intelligence/utils.py:130-139` (warning_pct/critical_pct), persiste a `intelligence_signals`/`metric_baselines` y los publica en `control_room_items` (`intelligence/persistence.py:347-449`). Severidad de anomalías Replicon recalculada con umbrales del workspace `control_room/api.py:783-806`. NO hay señales hardcodeadas; el demo seed NO siembra items. | El engine NO corre solo: `run_intelligence` solo se dispara por `POST /api/intelligence/run` (`routers/intelligence.py:117-131`); no hay DAG/cron (grep en `airflow/dags/` = 0 hits). Sin pipeline corrido, la ruta (1) muestra items `source_state` ("fuente no disponible") en vez de anomalías. |
| **Investigación** | **PARCIAL** | Evidence pack real-derivado: muestra de fila + historia + `sample_hash` + row_count (`intelligence/evidence.py:8-49`), persistido en `evidence_packs`/`evidence_items` (`98_intelligence_engine.sql:68-101`; `persistence.py:210-263`). Readiness verifica tablas Gold con conteos bajo RLS (`intelligence/readiness.py:172-239`); freshness por fuente `checked_at` (`api.py:556-601`) y por extracción (`routers/dashboard.py:172-199`, fresh/stale/very_stale). | El `query_text` citado es una **plantilla ilustrativa, no la query ejecutada** (`evidence.py:29-32`; `api.py:610-614` construye `SELECT * FROM dataset WHERE id='x' LIMIT 50` como string). Hipótesis = texto plantillado con ajustes heurísticos de confianza (`intelligence/hypotheses.py:27-127`). Para anomalías de dataset, `root_cause`/`recommendation` salen del diccionario estático `ANOMALY_COPY` (`core.py:930-971`, `api.py:333-341`). No hay chequeo de frescura por evidencia individual. |
| **Opciones** | **PARCIAL** | Ruta intelligence: score computado `score = impacto*confianza − costo − riesgo − tiempo` (`intelligence/scoring.py:8-43`); impacto sale de la desviación real; persistido en `decision_options` (`persistence.py:301-344`). | (a) costo/riesgo/tiempo son **constantes estáticas del YAML** (`config/intelligence_contracts/replicon.yaml:43-64`: cost 250/900/0, risk 120/350/50), no calculadas de datos. (b) Para items NO-intelligence las 3 opciones son **hardcodeadas** con score 92/68/45, riesgo "Bajo/Medio/Alto", tiempo "1-2 ciclos" (`control_room/state.py:522-558`); solo `money` se computa del impacto (`state.py:517-521`). Las opciones reales solo sustituyen a las fake si el item trae `intelligence.options` (`state.py:560-578`). |
| **Decisión** | **REAL** | `POST /items/{id}/decision` → `INSERT INTO decisions` + `decision_actions` + item→`decision_created` + `control_room_item_events` + `audit_events` con `critical=True` (`control_room/execution.py:622-709`). Approve: `execution.py:2411-2507` (decision_actions, item `approved`, lecciones, audit). Workspace-scoping verificado contra `decisions.workspace_id` (`execution.py:2433-2439`, `1657-1674`; `33_decisions_workspace_id.sql`). RBAC: `control_room.write` exigido en TODOS los POST (`routers/control_room.py:346-368,510-557`); roles con permiso: owner/super_admin/admin/workspace_admin/tenant_admin (`services/permissions.py:155-200`); analyst/viewer NO (`permissions.py:201-216`). CSRF en todas las mutaciones. Test runtime: `console/tests/test_control_room_permissions.py:83-95`. | `decisions` no tiene approver separado del creador (self-approve posible con un solo rol). |
| **Ejecución** | **REAL (gates) / PARCIAL (sustancia)** | Gate-chain real en `execute_item` (`execution.py:2205-2389`): permiso extra `control_room.execute` (router `control_room.py:475-481`), confirmación explícita (2264-2276), `decision_id` requerido (2278-2290), item no terminal (2292-2304), **dry-run validado requerido** (2340-2352), idempotencia con advisory lock + lookup (`execution.py:1633-1655,1527-1580`). Única plantilla interna ejecutable: `SUPPORTED_INTERNAL_WRITEBACK_TEMPLATES = {"create_followup_task"}` (`core.py:85`) → INSERT real en `decision_actions` + `control_room_action_executions(status='executed')` + evento + audit (`execution.py:1621-1792`), mensaje explícito "No se escribio en ERP". | (a) **El dry-run no valida nada**: `action_dry_run` siempre devuelve `{"validated": True}` sin tocar sistema alguno (`execution.py:1250-1257`) — es un paso de workflow, no una prueba. (b) Los adapters nunca se invocan con `dry_run=True` desde el flujo (solo `dry_run=False` en `execution.py:2001`). (c) No se dispara ningún MCP tool/DAG/job en ejecutar — el único efecto es una fila en `decision_actions`. (d) 12 plantillas definidas (`core.py:182-314`) pero solo 3 con adapter (`core.py:121-127`: Replicon ×2, SAP HCM ×1); el resto da 501 `adapter_missing` incluso con el flag encendido. (e) Tabla `control_room_action_templates` (91_…sql:30-44,112-130) **nunca es leída por el código** (grep = 0): el runtime usa el dict Python — schema-teatro. |
| **Write-back externo (bloqueo)** | **REAL — bloqueado por diseño y auditado** | Flag default false: `CONTROL_ROOM_ENABLE_EXTERNAL_WRITEBACK` (`execution.py:33-39`; no presente en `infra/docker-compose.yml`). Bloqueo explícito: `execution.py:2236-2248` → `_record_execute_block` persiste `control_room_action_executions(status='blocked')` + `execution_status='blocked'` + evento `action_blocked` + `audit_events action='control_room.action.execute.blocked'` (`execution.py:1471-1523`) + HTTP 409. NO se presenta como ejecutado (UI muestra "Ejecución supervisada bloqueada", `page.tsx:1433`). El código de adapter SÍ es real (HTTP POST + circuit breaker + Idempotency-Key: `adapters/replicon_adapter.py:79-137`), con audit preflight fail-closed (`execution.py:1968-1989`) — listo pero apagado. UI advierte "No se escribirá en ERP/SAP externo" (`page.tsx:1429`). | — (cumple el diseño P1-CR-001). |
| **Control / seguimiento** | **PARCIAL** | Bitácora real: timeline desde `control_room_item_events` + `control_room_action_executions` + `decision_actions` (`state.py:1239-1315`; UI `page.tsx:920-932`). `ops_summary` agrega ejecuciones por status (`api.py:1856-1861`). Checklist persistente vía `update_item_control` (`execution.py:841`, estado en metadata `state.py:423-499`). | El checklist son 2 controles plantilla fijos ("refresh", "audit") con cierre **manual** (`state.py:441-472`). No hay loop de verificación post-ejecución: nadie re-chequea si la métrica se recuperó ni si el followup se completó. La cadencia "Proximo refresh operativo" es texto (`state.py:696`). |
| **Lecciones** | **REAL (almacenar) / PARCIAL (re-aplicar)** | Almacén real `control_room_lessons`: manual (`execution.py:966-1042`), al aprobar (`execution.py:2484`), al éxito de adapter (`execution.py:1858-1921`), desde outcome del engine (`persistence.py:703-718`). Re-aplicación: lecciones se cruzan con items futuros por cartridge+anomaly_type (`state.py:243-281`, invocado en dashboard `api.py:1572-1574`), suben priority_score +4 c/u hasta +12 (`execution.py:189-193`), y generan `suggested_actions` desde lecciones de adapter (`state.py:193-239`). Aplicar lección = endpoint con persistencia + audit (`execution.py:1046-1163`). `prediction_outcomes` guarda error de predicción (`persistence.py:680-702`). | (a) Al aprobar sin lecciones se persisten **2 lecciones boilerplate** ("Si {dataset} genera {tipo}, abrir revision OMEGA…", `api.py:1175-1178` → `execution.py:2484`) — infla el "aprendizaje" con plantillas. (b) `prediction_outcomes` solo se muestra (`persistence.py:540-551`, `routers/metrics.py:281,356`): NO recalibra baselines, confianza ni scores de opciones futuras. |

---

## 2. Matriz de acciones (clic → ¿qué pasa?)

UI = `console-next/src/app/(shell)/control-room/page.tsx` (3.499 líneas, wired real); endpoints = `console/app/routers/control_room.py`; backend = `console/app/services/control_room/*`.

| Acción | UI | Endpoint | Backend ejecuta | MCP/tool real | DB cambia | Audit event | RBAC/approval | Veredicto |
|---|---|---|---|---|---|---|---|---|
| Ver dashboard/señales | ✔ page.tsx:894 | ✔ GET /dashboard (router:111) | ✔ fetch Gold+normalize (api.py:1545-1748) | lectura via refinement/mcp (api.py:83) | no (persist overlay items) | no | datasets.read | **REAL** |
| Ack/snooze/assign/falso-positivo alerta | ✔ page.tsx:1563-1589 | ✔ router:136-221 | ✔ `_operate_alert` persiste estado (execution.py:2649-2851) | n/a | ✔ items.metadata+events | ✔ | control_room.write+CSRF | **REAL** |
| Registrar paso OMEGA | ✔ page.tsx:1339-1361 | ✔ /step (router:244) | ✔ record_item_step (execution.py:790) | n/a | ✔ events | ✔ | write+CSRF | **REAL** |
| Seleccionar opción | ✔ page.tsx:1363 | ✔ /option (router:386) | ✔ execution.py:713-775 | n/a | ✔ selected_option_id | ✔ | write+CSRF | **REAL** (pero opciones fake en items no-intel) |
| Crear decisión | ✔ page.tsx:1378 | ✔ /decision (router:366) | ✔ INSERT decisions+actions (execution.py:622-709) | n/a | ✔ | ✔ critical | write+CSRF | **REAL** |
| Aprobar | ✔ page.tsx:1461 | ✔ /approve (router:535) | ✔ execution.py:2411-2507 | n/a | ✔ | ✔ critical | write+CSRF | **REAL** (auto-lecciones boilerplate) |
| Preview | ✔ page.tsx:1390 | ✔ /action-preview (router:409) | estado+payload, sin efecto (execution.py:1178-1229) | no | ✔ action_executions(preview) | ✔ critical | write+CSRF | **PARCIAL** (solo registro) |
| Dry-run | ✔ page.tsx:1406 | ✔ /action-dry-run (router:432) | **siempre `validated:true`, no prueba nada** (execution.py:1250-1257) | no | ✔ action_executions(validated) | ✔ critical | write+CSRF | **FAKE como validación / REAL como gate** |
| Ejecutar (create_followup_task) | ✔ page.tsx:1422-1447 (confirm + idempotency_key) | ✔ /execute (router:475) | ✔ INSERT decision_actions (execution.py:1676-1692) | no MCP; escritura interna directa | ✔ 3 tablas | ✔ audit tx-bound (execution.py:1764-1782) | write+**execute**+CSRF+confirm+decision+dry-run | **REAL (interno, alcance mínimo)** |
| Ejecutar (plantillas ERP) | ✔ (botón visible, capability lo desactiva) | ✔ mismo endpoint | bloqueado 409/501 + registrado (execution.py:2222-2262) | adapter HTTP real existe pero apagado (replicon_adapter.py:93-94) | ✔ status='blocked' | ✔ `…execute.blocked` | idem | **BLOQUEADO POR DISEÑO (honesto)** |
| Modo automático | ✔ page.tsx:1449 | ✔ /auto-run (router:455) | ✔ investigación→opción→decisión→preview→dry-run, **se detiene antes del write-back** (execution.py:1295-1418, `stopped_before_writeback: true`) | no | ✔ | ✔ critical | write+CSRF | **REAL** (bug: opción 'remediate' hardcodeada, execution.py:1319-1326) |
| Umbrales upsert | ✔ page.tsx:1312 | ✔ POST/PATCH /thresholds (router:611-648) | ✔ state.py:1542-1611 | n/a | ✔ control_room_thresholds | ✔ | write+CSRF | **REAL** (afecta severidad: api.py:785-806) |
| Lección crear/aplicar | ✔ page.tsx:1511-1561 | ✔ router:271-314 | ✔ execution.py:966-1163 | n/a | ✔ control_room_lessons | ✔ critical | write+CSRF | **REAL** |
| Outcome intelligence | ✔ page.tsx:1526-1545 | ✔ /signals/{id}/outcome (intelligence.py:193-208) | ✔ persistence.py:639-747 | n/a | ✔ prediction_outcomes+lessons | ✔ | write+CSRF | **REAL** (sin recalibración) |
| Run intelligence | (no en página control-room) | ✔ POST /api/intelligence/run (intelligence.py:117-131) | ✔ engine completo | lectura Gold directa | ✔ 6 tablas | ✔ intelligence.run | write+CSRF | **REAL pero manual** (sin scheduler) |

---

## 3. Workspace / Studio / Marketplace

**Workspace (:8001) — REAL.** Apps analíticas servidas desde DB con sandbox CSP (`workspace/app/main.py:574-636`; app real `static/apps/pnl_ejecutivo.html` con chart.js); data API con SQL parametrizado a Gold vía refinement (`main.py:815-950`); asistente de consumo con Anthropic real + streaming tool_use, whitelist read-only (`workspace/app/services/llm_client.py:240-248`, `consumer_assistant.py:30-47`); decisiones CRUD reales con RLS (`main.py:1077-1236`). CORS: wildcard **rechazado** explícitamente, default solo `http://localhost:8000` (console), prod requiere `ALLOWED_ORIGINS` (`main.py:138-141`). Auth: cookie validada contra `user_sessions` + tope 12h (`services/session.py:144-190`), no header-trust; CSRF double-submit testeado (`workspace/tests/test_csrf.py`).

**Studio — REAL en backend (~90%), FACHADA en console-next.** Goal runs durables con approval gates secuenciales por SQL (`infra/init/95_studio_goal_runs.sql:10-24`; `services/studio_goal_runs.py:235-499`); 9/10 ejecutores de pasos reales (introspección viva con fallback estático declarado, `routers/studio.py:818-845`; smoke de extracción invoca MCP infra real `studio.py:1641-1731`; el ejecutor fallback es noop "completed"). Entidades persistidas + sync a `entity_config` (`58_studio_entities.sql:9-18`; `studio_entities.py:268-280`). Asistente con Claude real vía `llm_client` (`studio_assistant.py:957`). El frontend nuevo lo admite: comentario "the full /api/studio/* surface is 100 % stubs" y deep-link al UI legacy :8000 (`console-next/src/components/studio/CartridgeLauncherCard.tsx:52-65`).

**Marketplace — REAL: instalar SÍ cambia comportamiento.** Catálogo seedeado desde `cartridges` (`73_marketplace_installations.sql:103-123`). Flujo: request → `marketplace_orders`+`tenant_entitlements(requested)` (`routers/marketplace.py:84-102`) → approve admin → `cartridge_installations(ready)`+entitlement activo (`marketplace_service.py:558`). La prueba clave: `dependencies.py:74-128` consulta entitlements+installations para poblar `allowed_cartridges`, que filtra Control Room (`routers/control_room.py:34`), Intelligence (`intelligence/engine.py:48-52`), visibilidad de cartuchos y prefijos S3/lake (`security_context.py:196-229`). Revoke real (`marketplace.py:223-227`, `marketplace_service.py:737-748`). Sin pagos/licencias: máquina de estados de aprobación, todo gratis.

---

## 4. Demo mode — dependencia del seed

`infra/init_dev/16_demo_control_room_seed.sql` siembra exactamente **1 umbral** (replicon/low_margin, líneas 39-52) y **1 lección** (55-69), etiquetados `metadata.demo=true`, solo dev (compose guard `infra/docker-compose.yml:49-52`; skip si APP_ENV=production). `15_local_dev_bootstrap.sql` siembra usuarios/entitlements/installations (líneas 7,54,80) — sin esto NO hay módulos activos en el cockpit.

**Sin demo seed:** items/alertas/fuentes NO dependen de él (se computan de Gold en runtime — comentario explícito del seed, líneas 3-9); los paneles Lecciones/Umbrales quedan vacíos pero funcionales. **El verdadero dependiente es el pipeline:** sin extracciones Gold el cockpit muestra items `source_state` ("fuente no disponible") y 0 anomalías. Las *definiciones* de datasets Gold vienen seeded de un dump AWS (`infra/init/10_replicon_gold_seed.sql:1-3` "Auto-generated… from 10.0.2.175"). `ops_summary` expone `has_demo_seed` por env (`api.py:1895`).

---

## 5. Tablas reales del ciclo (verificadas en uso)

| Concepto | Tabla | DDL | Escrita por | Leída por |
|---|---|---|---|---|
| Items/señales | `control_room_items` | 90:7-33 + 91:6-22 | persistence.py:377, state.py:1431, execution.py (múltiple) | api.py:1186, state.py:941 |
| Eventos del item | `control_room_item_events` | 90:45-58 | state.py:1319-1349 | state.py:1252 |
| Action runs | `control_room_action_executions` | 91:49-67 | api.py:1917-1953 | state.py:1267, execution.py:1539 |
| Plantillas de acción | `control_room_action_templates` | 91:30-44 | seed 91:112-130 | **NADIE (muerta; runtime usa core.py:182-314)** |
| Umbrales | `control_room_thresholds` | 91:75-89 | state.py:1542 | state.py:81-103 |
| Lecciones | `control_room_lessons` | 91:95-107 | execution.py:1893, persistence.py:704 | state.py:112-148 |
| Decisiones | `decisions`/`decision_actions` | 04:4-31, 33:7-18 | execution.py:652-671,1683 | execution.py:2433, state.py:1284 |
| Auditoría | `audit_events` | 16:4-17 (+39,44,92) | audit_service.py:100-161 (fail-closed si critical), execution.py:1449 | dashboard.py:247-276 |
| Intelligence | `metric_baselines`, `intelligence_signals`, `evidence_packs`, `evidence_items`, `hypotheses`, `decision_options`, `prediction_outcomes` | 98:4-162 | persistence.py:92-344,680 | persistence.py:452-563 |
| Externo/predictivo | `external_intelligence_sources`, `external_evidence_cache`; cols predicción | 99:3-58 | intelligence/external.py | external.py, baseline.py |

---

## 6. Findings

| ID | Sev | Finding | Evidencia | Estado |
|---|---|---|---|---|
| **P0-CR-101** | P0 | **"Dry-run validado" es teatro de validación**: siempre `ok/validated:true` sin probar credenciales, conexión ni payload contra nada; es prerequisito del execute, dando falsa confianza de que "se validó". Los adapters tienen modo dry_run real (replicon_adapter.py:89) que NUNCA se usa. | execution.py:1233-1291 (resultado fijo 1250-1257); adapter solo con dry_run=False en 2001 | Abierto |
| **P1-CR-102** | P1 | **Opciones con scores inventados para items no-intelligence**: 92/68/45, riesgo "Bajo/Medio/Alto", tiempo "1-2 ciclos" hardcodeados; el usuario decide sobre números fabricados. | state.py:522-558 | Abierto |
| **P1-CR-001** | P1 (del diagrama) | Write-back externo bloqueado por flag — **verificado implementado correctamente**: bloqueado, auditado (`…execute.blocked`), persistido como `blocked`, HTTP 409, nunca mostrado como ejecutado; adapters reales listos detrás del flag. | execution.py:33-39, 2236-2248, 1471-1523 | Cerrado/Confirmado |
| **P1-CR-103** | P1 | **Ejecución interna = 1 sola acción con efecto mínimo**: `create_followup_task` inserta una fila en `decision_actions`. Ninguna acción dispara MCP tool, DAG, job ni corrección de datos. 9 de 12 plantillas no tienen adapter (501 siempre). | core.py:85,121-127,182-314; execution.py:1676-1692 | Abierto |
| **P2-CR-104** | P2 | **Lecciones boilerplate auto-persistidas al aprobar** sin lecciones reales → infla métricas de aprendizaje con plantillas. | api.py:1175-1178 → execution.py:2484 | Abierto |
| **P2-CR-105** | P2 | **auto-run rompe con señales intelligence**: selecciona opción hardcodeada `"remediate"`, inexistente cuando las opciones reales del engine reemplazan a las estáticas → 400 "invalid control room option". | execution.py:1319-1326 vs state.py:560-580 + execution.py:723-726 | Abierto (bug) |
| **P2-CR-106** | P2 | **Sin scheduler de intelligence**: señales del engine solo bajo demanda (POST /run); sin cron las señales envejecen y el "ciclo continuo" depende de clics. | routers/intelligence.py:117-131; airflow/dags/ sin referencias | Abierto |
| **P2-CR-107** | P2 | **`control_room_action_templates` (DB) es schema muerto**: seeded en 91 pero el runtime usa el dict Python; editar la tabla no cambia nada. | 91:30-130 vs core.py:182-314; grep `FROM control_room_action_templates` = 0 | Abierto |
| **P2-CR-108** | P2 | **Control sin verificación automática**: checklist de 2 ítems manual; outcomes (`prediction_outcomes`) nunca recalibran baselines/opciones; no hay re-chequeo de recuperación de la métrica. | state.py:441-472; persistence.py:540-551 solo lectura | Abierto |
| **P3-CR-109** | P3 | Citas SQL ilustrativas (no ejecutadas tal cual) en evidencia e items; root_cause de anomalías = copy estática por tipo. | evidence.py:29-32; api.py:610-614; core.py:930-971 | Abierto |
| **P3-CR-110** | P3 | Decisión sin segregación creador/aprobador (un mismo `control_room.write` crea y aprueba). | execution.py:2411-2429 | Abierto |

### Fortalezas verificadas (no fachada)
- Cadena de gates de ejecución completa y testeada: confirm → decision → dry-run → idempotencia (advisory lock) → permiso dedicado `control_room.execute` (execution.py:2205-2389; test_control_room_permissions.py:30-95).
- Auditoría fail-closed en operaciones críticas (audit_service.py:74-91,162-165) y audit preflight transaccional antes de write-back externo (execution.py:1968-1989).
- Todo workspace/tenant-scoped con RLS en Gold (gold_fetcher.py:104-127) y umbrales por workspace que sí cambian severidad (api.py:785-806).
- Frontend nuevo 100% conectado a endpoints reales con estados honestos ("bloqueada", "No se escribirá en ERP").
