{
  "name": "omega-tests-e2e",
  "version": "0.1.0",
  "private": true,
  "description": "OMEGA Playwright E2E suite — detection-only sprint v1.44.3.2. Browser-driven smoke + functional tests against the Next.js console, the legacy HTML console, the FastAPI backend, and external service surfaces.",
  "scripts": {
    "test": "playwright test",
    "test:list": "playwright test --list",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "@types/node": "^20.16.11"
  }
}
