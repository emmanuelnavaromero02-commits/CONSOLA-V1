# v1.43.2 (DevOps R1 follow-up): bumped fastapi 0.115.0 → 0.121.0
# so the transitive starlette range moves to >=0.40.0,<0.50.0 —
# escaping CVE-2024-47874 (multipart DoS) AND GHSA-2c2j-9gv5-cj73 +
# GHSA-7f5h-v6xp-fcq8 (starlette 0.47/0.49 fixes). fastapi 0.115
# capped starlette at <0.39 and 0.116 at <0.47 — both still vulnerable
# to the later GHSAs. starlette is explicitly re-pinned below.
fastapi==0.121.0
starlette>=0.49.1
uvicorn[standard]==0.30.6
httpx==0.27.2
asyncpg==0.29.0
anthropic==0.49.0
google-genai==1.2.0
openai==2.36.0
pyyaml==6.0.2
minio==7.2.9
# v1.43.2 (DevOps R1 follow-up): python-multipart 0.0.9 had 4 CVEs
# (GHSA-59g5-xgcq-4qw3, GHSA-wp53-j4wj-2cfg, GHSA-mj87-hwqh-73pj,
# GHSA-pp6c-gr5w-3c5g). 0.0.27 escapes all four.
python-multipart==0.0.27
bcrypt==4.1.3
python-jose[cryptography]==3.5.0
redis==5.0.8
# Test-only: fakeredis backs console/tests/test_jwt_blacklist.py. Listed here
# (not in a separate requirements-dev.txt) so the CI workflow — which only
# installs console/requirements.txt before running console/tests/ — sees it.
fakeredis==2.35.1
