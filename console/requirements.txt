# v1.43.2 (DevOps R1 follow-up): bumped fastapi 0.115.0 → 0.116.0
# so the transitive starlette range moves to >=0.40.0,<0.47.0 —
# escaping CVE-2024-47874 (multipart DoS) and the path-traversal
# vuln in starlette 0.37/0.38. fastapi 0.115's range capped
# starlette at <0.39 (= still vulnerable). starlette is explicitly
# re-pinned below to defend against future fastapi range broadening.
fastapi==0.116.0
starlette>=0.40.0
uvicorn[standard]==0.30.6
httpx==0.27.2
asyncpg==0.29.0
anthropic==0.49.0
google-genai==1.2.0
openai==2.36.0
pyyaml==6.0.2
minio==7.2.9
python-multipart==0.0.9
bcrypt==4.1.3
python-jose[cryptography]==3.5.0
redis==5.0.8
# Test-only: fakeredis backs console/tests/test_jwt_blacklist.py. Listed here
# (not in a separate requirements-dev.txt) so the CI workflow — which only
# installs console/requirements.txt before running console/tests/ — sees it.
fakeredis==2.35.1
