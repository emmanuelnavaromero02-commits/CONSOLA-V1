# v1.43.2 (DevOps R1 follow-up): see console/requirements.txt for the
# starlette + fastapi CVE chain rationale.
fastapi==0.136.1
starlette>=1.0.1
# Keep mcp-infra aligned with the other FastAPI services so a combined
# developer/test install resolves cleanly.
uvicorn[standard]==0.47.0
httpx==0.28.1
psycopg2-binary==2.9.9
minio==7.2.9
# v1.43.5: pyarrow 23.0.1 closes PYSEC-2026-113 while keeping the
# dataset/RAG parquet API surface used here unchanged.
pyarrow==23.0.1
pandas==2.1.4
pydantic-settings==2.14.1
duckdb==1.2.2
sqlalchemy==2.0.34
asyncpg==0.29.0
pgvector==0.3.2
# v1.43.2 (DevOps R1 follow-up): pypdf 4.3.1 had 13+ CVEs (parser DoS,
# loop bombs, unbounded recursion); 6.10.2 closes the full chain.
# Public API used by mcp-infra (PdfReader, page extraction) is
# unchanged across the 4 -> 6 major bump per pypdf's stability policy.
pypdf==6.10.2
boto3>=1.34.0
