# v1.43.2 (DevOps R1 follow-up): bumped fastapi 0.111.0 → 0.116.0 so
# the transitive starlette pin moves to >=0.40.0 — escaping
# CVE-2024-47874 (multipart DoS) and the path-traversal vuln in
# starlette 0.37/0.38. fastapi 0.115's range capped starlette at
# <0.39 (= still vulnerable). starlette is explicitly re-pinned
# below to defend against future fastapi range broadening.
fastapi==0.116.0
starlette>=0.40.0
uvicorn[standard]==0.29.0
httpx==0.27.0
psycopg2-binary==2.9.9
minio==7.2.9
pyarrow==16.1.0
pandas==2.1.4
pydantic-settings==2.2.1
duckdb==1.1.0
sqlalchemy==2.0.34
asyncpg==0.29.0
pgvector==0.3.2
pypdf==4.3.1
google-genai==1.2.0
