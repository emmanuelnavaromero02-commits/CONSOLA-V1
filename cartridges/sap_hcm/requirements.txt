# v1.43.2 (DevOps R1 follow-up): see console/requirements.txt.
fastapi==0.136.1
starlette>=1.0.1
# v1.43.4 — uvicorn floor bumped 0.30.6 → 0.35.0 because fastmcp 3.x
# server extras require uvicorn >= 0.35. Range is intentionally open
# so transitive resolution can pick a compatible newer release.
uvicorn[standard]>=0.35.0,<1.0
# v1.43.4 — Bumped from 2.32.3 (closes GHSA-9hjg-9r4m-mvj7, GHSA-gc5v-m9x4-r6x2).
requests>=2.33.0,<3.0
# v1.43.4 — Bumped pydantic 2.9.2 → 2.11.7 (floor) and
# pydantic-settings 2.6.1 → 2.7 (floor) because fastmcp 3.x
# requires pydantic >= 2.11.7. The cartridge code uses only the
# stable Pydantic v2 BaseModel surface, so the bump is API-safe.
pydantic>=2.11.7,<3.0
pydantic-settings>=2.7.0,<3.0
# v1.43.4 — Bumped from 1.1.0 (closes GHSA-mf9w-mj56-hr94).
python-dotenv>=1.2.2,<2.0
sqlalchemy==2.0.35
psycopg2-binary==2.9.9
minio==7.2.9
pandas==2.2.3
pyarrow==23.0.1
pyyaml==6.0.2
cryptography==50.0.0
duckdb==1.2.2
# v1.43.4 — Bumped fastmcp 2.5.0 → 3.x to fix C1 BLOCKER:
# the cartridge's /mcp/tools handler calls ``mcp.list_tools()`` and
# ``mcp.get_tool()``, which only exist in fastmcp 3.x. Pinning at
# 2.5.0 produced HTTP 500 with ``AttributeError: 'FastMCP' object
# has no attribute 'list_tools'`` on every /mcp/tools request,
# making the cartridge invisible to the console's MCP registry.
# Replicon already runs on fastmcp 3.x (it was unpinned in
# v1.43.1+), which is why Replicon's MCP contract works while
# SAP cartridges did not.
#
# The 3.x bump also closes the six fastmcp CVEs (GHSA-mxxr-jv3v-6pgc,
# GHSA-rj5c-58rq-j5g5, GHSA-rcfx-77hg-w2wv, GHSA-5h2m-4q8j-pqpj,
# GHSA-m8x7-r2rg-vh5g, GHSA-rww4-4w9c-7733) that v1.43.2 had to
# suppress via --ignore-vuln in .github/workflows/security.yml.
fastmcp>=3.3.0,<4.0
asyncpg==0.29.0
httpx==0.28.1
