# v1.43.2 (DevOps R1 follow-up): see console/requirements.txt.
# Replicon was unpinned; pin fastapi explicitly so the starlette
# floor stays satisfiable.
fastapi>=0.121.0
starlette>=0.49.1
uvicorn[standard]
requests
pydantic
pydantic-settings
python-dotenv
sqlalchemy
psycopg2-binary
minio
pandas
pyarrow
pyyaml
cryptography
duckdb
# v1.43.4 — Replicon was unpinned, which worked by accident
# (pulled fastmcp 3.x naturally) but left the cartridge exposed
# to a future fastmcp 4.x breaking change with no version gate.
# Pin to the same range as the SAP cartridges so all 4 are
# locked to a single major.
fastmcp>=3.3.0,<4.0
asyncpg
