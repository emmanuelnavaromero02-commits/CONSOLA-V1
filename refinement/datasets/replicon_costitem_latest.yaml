name: replicon_costitem_latest
description: 'CostItem - Ultima extraccion Replicon Bronze'
layer: silver
sources:
- raw/replicon/CostItem
sql: "SELECT * FROM read_parquet('s3://lakehouse/raw/replicon/CostItem/**/*.parquet', hive_partitioning=true, union_by_name=true)"
