name: replicon_task_latest
description: 'Task - Ultima extraccion Replicon Bronze'
layer: silver
sources:
- raw/replicon/Task
sql: "SELECT * FROM read_parquet('s3://lakehouse/raw/replicon/Task/**/*.parquet', hive_partitioning=true, union_by_name=true)"
