name: replicon_resourcerequest_latest
description: 'ResourceRequest - Ultima extraccion Replicon Bronze'
layer: silver
sources:
- raw/replicon/ResourceRequest
sql: "SELECT * FROM read_parquet('s3://lakehouse/raw/replicon/ResourceRequest/**/*.parquet', hive_partitioning=true, union_by_name=true)"
