name: replicon_department_latest
description: 'Department - Ultima extraccion Replicon Bronze'
layer: silver
sources:
- raw/replicon/Department
sql: "SELECT * FROM read_parquet('s3://lakehouse/raw/replicon/Department/**/*.parquet', hive_partitioning=true, union_by_name=true)"
