name: replicon_timesheet_latest
description: 'Timesheet - Ultima extraccion Replicon Bronze'
layer: silver
sources:
- raw/replicon/Timesheet
sql: "SELECT * FROM read_parquet('s3://lakehouse/raw/replicon/Timesheet/**/*.parquet', hive_partitioning=true, union_by_name=true)"
