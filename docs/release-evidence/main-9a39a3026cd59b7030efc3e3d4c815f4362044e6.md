# Release Evidence — main 9a39a3026cd59b7030efc3e3d4c815f4362044e6

Base branch: `main`
Base commit: `9a39a3026cd59b7030efc3e3d4c815f4362044e6`
Base subject: `Fix Studio tests for scoped vault context`
Version at base: `1.45.3-beta`
Evidence date: `2026-06-04`

This evidence supports the current verdict:

- Beta privada/controlada: YES.
- v1.0 publica enterprise: NO, until P2 live gates are green.

## Current Hardening Branch Evidence

Branch: `codex/v1-hardening-gates`

### Compose Rate-Limit Split

Command:

```bash
printf 'base:\n'
docker compose --env-file infra/.env.example -f infra/docker-compose.yml --profile sap config | rg 'RATE_LIMIT_ENABLED' || true
printf 'dev override:\n'
APP_ENV=development ALLOW_RCE_TOOLS=true \
  docker compose --env-file infra/.env.example \
  -f infra/docker-compose.yml \
  -f infra/docker-compose.dev.yml \
  --profile sap config | rg 'RATE_LIMIT_ENABLED: "false"'
```

Output:

```text
base:
dev override:
      RATE_LIMIT_ENABLED: "false"
```

### Static P0 Guards

Command:

```bash
.venv/bin/pytest -q tests/test_rate_limit_bypass.py tests/test_smoke_script.py tests/test_ci_workflows_present.py
```

Output:

```text
68 passed in 9.42s
```

Command:

```bash
.venv/bin/pytest -q tests/test_local_bootstrap_reproducibility.py \
  tests/test_smoke_script.py::test_makefile_lifecycle_targets_manage_full_sap_stack \
  tests/test_apply_db_migrations_script.py
```

Output:

```text
10 passed in 0.09s
```

### Smoke

Command:

```bash
make smoke
```

Output:

```text
[smoke] ✅ 47/47 checks passed
```

### Browser E2E

Command:

```bash
OPEN_REPORT=0 make e2e
```

Output:

```text
3 skipped
357 passed (4.1m)
```

### Local Reset From Clean Volumes

Command:

```bash
make nuke CONFIRM=NUKE NUKE_SCOPE=local-dev && \
APP_ENV=development ALLOW_RCE_TOOLS=true make up && \
bash scripts/wait_for_health.sh
```

Output:

```text
[wait_for_health] console=healthy postgres=healthy postgres_gold=healthy superset=healthy salesforce=healthy hubspot=healthy sap_hcm=healthy sap_s4=healthy sap_sf=healthy api=1 core=1 external=1 restarting=none
[wait_for_health] Stack ready.
```

### Local Repair Helper

Command:

```bash
LOCAL_REPAIR_SCOPE=local-dev bash scripts/local_stack_repair.sh
```

Output:

```text
[local-repair] checking local Postgres role/password drift
[reconcile-db-passwords] rotating postgres superuser password
DO
[reconcile-db-passwords] rotating operational DB role passwords
DO
[reconcile-db-passwords] rotating gold postgres superuser password
DO
[reconcile-db-passwords] rotating gold DB role password
DO
[reconcile-db-passwords] done
[local-repair] Superset metastore repair not requested
[local-repair] To clear local encrypted Superset DB fields after a SECRET_KEY rotation, run:
  CONFIRM_SUPERSET_METASTORE_REPAIR=LOCAL_SUPERSET_REPAIR make repair-local-stack
```

## Prior Full-Stack Validation Evidence

These commands were run against the same base `main` after Docker Desktop was opened and local stale-volume password/Superset metastore drift was repaired.

### Test Suites

Command:

```bash
make test
```

Output:

```text
tests/                  2211 passed, 62 skipped
console/tests/           740 passed
refinement/tests/        130 passed
vault/tests/              41 passed
workspace/tests/          33 passed
cartridges/              203 passed
```

### Production Readiness

Command:

```bash
OMEGA_PRODUCTION_READINESS_SKIP_STRESS=1 make production-readiness
```

Output:

```text
[production-readiness] stress skipped by OMEGA_PRODUCTION_READINESS_SKIP_STRESS=1
[production-readiness] PASS
```

Included:

- docker compose config
- `scripts/wait_for_health.sh`
- `/readyz?require_data=1`
- Superset login
- scoped Vault / pipeline / RLS / ownership / API-v1 regression tests
- `make test`
- `make smoke`
- `make e2e`
- `make acceptance`

### Full-Stack Acceptance

Command:

```bash
make acceptance
```

Output:

```text
tests/e2e/test_console_full_stack_acceptance.py 4 passed
```

Covered:

- Console surfaces connected
- Studio / Copilot / Semantic / Knowledge surfaces
- HubSpot fake upstream extraction
- Bronze refresh
- Silver refresh
- Gold catalog/data query end-to-end

### Frontend

Commands:

```bash
npm --prefix console-next install
npm --prefix console-next run typecheck
npm --prefix console-next run lint
npm --prefix console-next run test
npm --prefix console-next run test:coverage
npm --prefix console-next run build
npm --prefix console-next run export:copy
```

Output summary:

```text
typecheck passed
lint passed
28 tests passed
build passed
test:coverage passed, with global coverage around 2%
```

### Security / Dependency Audits

Commands:

```bash
make security-scan
.venv/bin/bandit -r console workspace vault refinement mcp-infra cartridges --severity-level medium --confidence-level high
.venv/bin/pip-audit
npm --prefix console-next audit
```

Output summary:

```text
bandit high-confidence clean via .venv/bin/bandit
pip-audit: no known vulnerabilities via .venv/bin/pip-audit
npm audit: no vulnerabilities via npm --prefix console-next audit
```

## Explicit Skips / Not v1-Public Evidence

These did not run to completion and remain P2 gates:

- Stress/load without `OMEGA_PRODUCTION_READINESS_SKIP_STRESS=1`
- Multi-user simulation as a required production-readiness gate
- Live LLM prompt-injection / no-exfiltration probe
- Live cartridge credentials for HubSpot, Salesforce, SAP HCM, SAP S/4HANA, SAP SuccessFactors, Replicon
- AWS/HTTPS staging deployment and rollback rehearsal

Correct verdict while any item above remains unproven:

```text
Beta privada/controlada: YES
v1.0 publica enterprise: NO
```
